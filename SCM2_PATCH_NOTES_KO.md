# 소울카드마스터2 Android/WIE 패치 메모

## 이번 수정

`버전 확인중...` 상태(state 200)에서 확인되는 두 개의 분기 모두를 오프라인 성공 경로로 보냅니다.

- `0x0012db46`: 첫 번째 버전/인증 게이트 (`BNE` -> `B`)
- `0x0012dbfe`: 두 번째 상태 폴링 게이트 (`BEQ` -> `B`)

두 번째 게이트는 상태값이 0인 동안 매 프레임 그대로 반환하기 때문에, 옛 네트워크 응답이 오지 않으면 `버전 확인중...` 화면에 계속 머물 수 있습니다.

패치는 `data/binary_patches.toml`에서 이미지 로딩 시 패턴 기반으로 적용하며, `wie_ktf/src/runtime/java/jvm_support.rs`에도 FakeSocket 생성 시 동일한 안전 패치를 두었습니다.

## 빌드

기존 `.github/workflows/scm2-android.yml` 워크플로를 그대로 실행하면 됩니다.

> 이 수정은 정적 분석을 기반으로 한 테스트 패치입니다. 실제 기기에서 버전 확인 화면을 통과하는지 확인이 필요합니다.
